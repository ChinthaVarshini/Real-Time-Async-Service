{
  "name": "dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run server.ts --port=3001",
    "build": "bun build src/index.ts --outdir=dist --target=browser --minify",
    "serve": "bun run dist/index.js",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "fast-check": "^4.6.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "@types/node": "latest"
  }
}