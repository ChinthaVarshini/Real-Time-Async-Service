{
  "name": "worker",
  "version": "1.0.0",
  "scripts": {
    "start": "bun run index.ts",
    "test": "NODE_ENV=test bun test --run"
  },
  "dependencies": {
    "@types/nodemailer": "^7.0.11",
    "bullmq": "latest",
    "ioredis": "latest",
    "nodemailer": "^8.0.4",
    "zod": "latest"
  },
  "devDependencies": {
    "fast-check": "^4.6.0"
  }
}
